${COMMIT_MSG}

Container image is available at `ghcr.io/${REPO}:latest` or pin to this version with `ghcr.io/${REPO}:${CURRENT_TAG}`.

**Full Changelog**: https://github.com/${REPO}/compare/${PREVIOUS_TAG}...${CURRENT_TAG}

```
${VERSION_CONTENT}
```
